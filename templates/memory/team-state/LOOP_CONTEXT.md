# Loop Context（自动循环上下文）

**文件说明**：每次自动循环（cron / hook / 无人值守 workflow）开始前，必须先写入本文件，记录本次循环的触发来源与行为边界。orchestrator 在读取 STATE.md 之前先读本文件，确认本次循环被允许做什么、禁止做什么。

---

## 触发来源

- 触发类型: {{cron / hook / 人工手动}}
  <!-- 填写本次循环的发起方式：cron（定时任务）、hook（事件触发，如 CI 失败）、人（人工在主会话发起） -->
- 触发时间: {{YYYY-MM-DD HH:MM}}
  <!-- 本次循环开始的时间戳 -->
- 触发描述: {{如"每日 02:00 夜间巡检" / "CI pipeline #123 失败触发" / "用户手动启动 Audit"}}
  <!-- 一句话说明触发原因 -->

---

## 本次循环目标

- 循环目标: {{一句话描述本次循环要完成什么，如"对全仓跑 Audit 巡检，输出 NIGHTLY_AUDIT.md"}}
  <!-- 与 GOAL.md 的"完成条件"对应，但这里只填本次循环的局部目标 -->

---

## 允许的动作

- {{只读状态文件}}
- {{生成报告文件到约定落点}}
- {{运行检查脚本（只读，不修改代码）}}
<!-- 列出本次循环明确允许做的事。若不确定，保守填"只读 + 输出报告"。 -->

---

## 禁止的动作（默认含全部安全确认点动作）

以下动作在本次循环中**一律禁止**，不因循环目标或授权记录而豁免：

- 实际部署 / 热部署 / 重启线上服务
- 删除文件 / 目录 / 数据库表 / 数据
- git push / git reset --hard 等改写 git 历史的操作
- 向外部网络发送数据、推送镜像、对接生产环境凭据
- 修改业务代码（src/ / app/ 等目录）
- 修改契约文件（docs/API_CONTRACT.md / DB_SCHEMA.md 等）
<!-- 可追加本次循环特有的额外禁止项 -->
- {{本次循环额外禁止项，若无则删除本行}}

---

## 预算上限

- 轮次上限: {{填写，如"3 次"，沿用重试总规则}}
- 时间上限: {{填写，如"60 分钟"，超时熔断并写 RETRY_LOG.md}}
- Token 上限: {{填写，如"不限" / "100k tokens"；若环境不支持计量则填"不限（用时间兜底）"}}
<!-- 对应 GOAL.md 的预算字段，此处填写本次循环实际生效的上限值 -->

---

## 产物落点

- 报告文件: {{填写，如"docs/NIGHTLY_AUDIT.md"}}
- 状态写入: {{填写，如".claude/team-state/STATE.md / RETRY_LOG.md"}}
<!-- 本次循环允许写入的文件路径白名单；循环结束后产出必须在此范围内 -->
