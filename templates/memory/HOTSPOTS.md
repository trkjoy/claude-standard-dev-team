# HOTSPOTS — 跨项目高频失败模式聚合报告

<!-- 
  这是只读统计报告，由 kb-curator（aggregate 模式）通过 /team-kb-aggregate 命令生成。
  每次运行整体覆盖，不追加。
  本文件不自动修改任何 agent 提示词、验收标准或 harness 规则。
  反向修订 harness 是 P2 改造且必须由人确认，与本报告完全分离。
-->

> 生成时间: {YYYY-MM-DD HH:mm}
> 聚合来源: {N} 个项目（{M} 个文件成功读取，{K} 个文件跳过）
> 阈值: 出现 ≥{threshold} 次
> 高频模式总数: {X} 条

---

## 高频失败模式 Top 列表

<!-- 按出现次数降序排列。每条包含 5 个字段，填写时删除注释行。 -->

### 第 1 名

- **模式描述**: {归一化后的错误特征，不含具体变量值，例如"契约字段缺失：响应中缺少必填字段"}
- **出现次数**: {N}
- **涉及项目**: {来源项目路径列表，用逗号分隔}
- **归属 agent**: {从 RETRY_LOG 上下文推断的 agent 名称，推断不出填"未知"}
- **建议预防方向**: {1 句话建议，不超过 60 字，仅供人工参考，不自动写入任何 agent 定义}

### 第 2 名

- **模式描述**: 
- **出现次数**: 
- **涉及项目**: 
- **归属 agent**: 
- **建议预防方向**: 

<!-- 继续追加，格式同上 -->

---

## 跳过项目列表

<!-- 聚合时因文件缺失或路径错误被跳过的项目 -->

| 项目路径 | 跳过原因 |
|----------|----------|
| {path}   | {RETRY_LOG.md 不存在 / LEARNINGS.md 不存在 / 路径不存在} |

---

## 说明

- **本报告只读**：不得手动修改后当作"改进指令"直接写入 agent 提示词。如需基于本报告反向修订 harness，须走人工确认流程（P2 改造），确认后由人手动更新对应 agent 定义文件。
- **待验证假设**：跨项目聚合依赖各项目统一使用 `.claude/team-state/` 路径。若团队有多种路径约定，请在调用 `/team-kb-aggregate` 时提供完整绝对路径。
- **重新生成**：每次运行 `/team-kb-aggregate` 会整体覆盖本文件（非追加），旧快照不保留。如需历史对比，请在覆盖前手动备份。
