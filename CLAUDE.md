# CLAUDE.md

减少LLM编码常见错误的行为准则。根据需要与项目特定说明合并。

权衡取舍：这些指导原则倾向于谨慎而非速度。对于琐碎的任务，请自行判断。

1. 编码前先思考

不要妄下断言。不要掩盖困惑。要权衡利弊。

实施前：
- 明确陈述你的假设。如有疑问，请提出。
- 如果存在多种解释，请提出它们——不要默默地做出选择。
如果存在更简单的方法，请提出来。必要时要坚持己见。
如果有什么不清楚的地方，停下来。说出让你困惑的地方。然后提问。

## 2. 化繁为简

**解决问题所需的最小代码量。不包含任何推测性代码。**

- 没有超出客户要求的功能。
- 不支持一次性代码的抽象。
- 没有提供任何未要求的“灵活性”或“可配置性”。
- 对于不可能出现的情况，没有错误处理。
- 如果你写了 200 行，而 50 行就可以写完，那就重写。

问问自己：“一位资深工程师会认为这过于复杂吗？” 如果答案是肯定的，那就简化它。

3. 手术改变

只碰你必须碰的东西。只收拾你自己的烂摊子。

编辑现有代码时：
- 不要“改进”相邻的代码、注释或格式。
- 不要重构没有损坏的代码。
- 即使你的想法不同，也要保持与现有风格一致。
- 如果您发现无关的死代码，请提及它，不要删除它。

当你的更改创建了孤立文件时：
- 删除您修改后不再使用的导入/变量/函数。
- 除非被要求，否则不要删除已有的无效代码。

测试要求：每一行修改后的代码都应该直接追溯到用户的请求。

4. 目标驱动型执行

**定义成功标准。循环执行直至验证通过。**

将任务转化为可验证的目标：
- “添加验证” → “编写针对无效输入的测试，然后确保它们通过”
- “修复漏洞” → “编写一个能够重现该漏洞的测试，然后使其通过”
- “重构 X” → “确保重构前后测试均通过”

对于多步骤任务，请简要说明计划：
```
1. [步骤] → 验证：[检查]
2. [步骤] → 验证：[检查]
3. [步骤] → 验证：[检查]
```

明确的成功标准能让你独立循环执行。而模糊的标准（“只要能行就行”）则需要不断澄清。

**如果出现以下情况，则这些准则有效：**差异中不必要的更改较少，由于过于复杂而导致的重写较少，并且澄清问题在实施之前而不是在出错之后提出。